import maya.cmds as cmds

objSel = cmds.ls(sl = True)

objAmount = len(objSel)
newName = 'gggg'
padding = 4
startCount = '1'

for r in range(objAmount):
    for i in objSel:
        ren = cmds.rename( i, newName + '_' + startCount.zfill(padding))
        #print i
        nbr = ren.split('_')[-1]
        print nbr
        if nbr > padding:
           
